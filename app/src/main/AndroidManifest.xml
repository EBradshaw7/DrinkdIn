<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eoghan.drinkdin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CategoriesScreen" />
        <activity android:name=".CraftList" />
        <activity android:name=".CrawlScreen" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PorterhouseActivity"
            android:label="@string/title_activity_phouse_map" />
        <activity
            android:name=".CamdenStCrawl"
            android:label="@string/title_activity_wexford_st_crawl" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".NewUserActivity" />
        <activity android:name=".UserAreaActivity" />
        <activity android:name=".UserLocationActivity" />
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".SweetmansActivity" />
        <activity android:name=".BeerMarketActivity" />
        <activity android:name=".HeadlineActivity" />
        <activity android:name=".BlackSheepActivity" />
        <activity android:name=".GuinnessList" />
        <activity android:name=".BrazenHeadActivity" />
        <activity android:name=".TempleBarActivity" />
        <activity android:name=".StagsHeadActivity" />
        <activity android:name=".ArthursActivity" />
        <activity android:name=".GrogansActivity" />
        <activity android:name=".WhiskeyList" />
        <activity android:name=".DingleActivity" />
        <activity android:name=".CocktailList" />
        <activity android:name=".AlfiesActivity" />
        <activity android:name=".NearMeActivity" />
        <activity android:name=".AdditionalActivity"></activity>
    </application>

</manifest>